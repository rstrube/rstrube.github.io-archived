<!doctype html>
<html lang="en">

  {% include '_head' %}

  <body class="bg-light">

    {% include '_nav' %}
    
    <div class="container pt-5 pb-5">

      <h1>{{ page.title }}</h1>
      <div style="line-height:25px" class="mb-3">
        <div class="text-muted">{{ page.author }}</div>
        <div class="text-muted"><i class="bi-calendar align-text-bottom pr-2"></i><span class="align-text-top">{{ page.date }}</span></div>
      </div>

      <!-- Content here -->
      {{ page.content }}

    </div>
    
    {% include '_footer' %}

  </body>
</html>