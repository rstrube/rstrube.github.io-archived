<!doctype html>
<html lang="en">

  {% include '_head' %}

  <body class="bg-light">

    {% include '_nav' %}
    
    <div class="container pt-5 pb-5">

      <h1>{{ page.title }}</h1>
      <div style="line-height:25px" class="mb-3">
        <div class="text-muted"><a href="{{ page.author_url }}">{{ page.author }}</a></div>
        <div class="text-muted"><i class="bi-calendar pr-2"></i><span>{{ page.date }}</span></div>
      </div>

      <!-- Content here -->
      {{ page.content }}

    </div>
    
    {% include '_footer' %}

  </body>
</html>