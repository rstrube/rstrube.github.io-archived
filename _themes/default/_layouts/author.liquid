<!doctype html>
<html lang="en">

  {% include '_head' %}

  <body class="bg-light">

    {% include '_nav' %}
    
    <div class="container pt-5 pb-5">

      <div class="row align-items-center mb-4">
          <div class="col-auto">
            <img src="{{ page.author_image }}" class="d-block rounded-circle" style="width:100px" />
          </div>
          <div class="col-auto mr-auto">
            <h1>{{ page.title }}</h1>
          </div>
      </div>
      
      <!-- Content here -->
      {{ page.content }}

    </div>
    
    {% include '_footer' %}

  </body>
</html>