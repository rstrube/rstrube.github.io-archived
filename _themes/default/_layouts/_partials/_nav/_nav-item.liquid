<li class="nav-item active">
    <a class="nav-link" href="{{ nav.url }}">{{ nav.title }}
        {% if page.id == nav.id %}
            <span class="sr-only">(current)</span>
        {% endif %}
    </a>
</li>